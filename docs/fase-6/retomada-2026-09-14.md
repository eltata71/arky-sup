# Guía de retomada — sesión 2026-09-14 (F5 → F6)

**Para la próxima sesión:** lee esto primero. Todo lo necesario para arrancar
Fase 6 sin re-descubrir nada.

> Documento histórico de la sesión 2026-09-14. Sus conteos y referencias de
> Fase 5 describen el estado anterior; no son evidencia del cierre técnico
> actual de Fase 6.

## Dónde estamos

- Rama `main` en `eb703aa`, árbol limpio. Sin ramas de feature abiertas
  (solo 10 PRs Dependabot).
- **F5 cerrada con salvedad** (`docs/fase-5/cierre-fase-5.md`): datos OK,
  smoke sin auth OK, inferencia autenticada E2E **fallida** (429) y diferida.
- **F6 abierta** (`docs/fase-6/plan-fase-6.md`): backlog 6.0–6.6 con encaje al
  plan formal.

## Qué se verificó en esta sesión (evidencia real)

- Migraciones linkadas 18/18 en sync (`supabase migration list --linked`).
- `platform_settings_etl.py`: 6/6 tests OK.
- `npm run lint`: 0 errores.
- Smoke prod: `POST /api/ai` y `/api/gemini` → `401 missing_bearer_token` con
  `requestId` (gate responde, esperado sin auth).
- **No verificado:** `typecheck` y suite Vitest completa (timeouts del entorno,
  120–180 s). CI en `main` rojo por infra (jobs mueren en 2–3 s, `runner_name`
  vacío, sin logs) — no por código.
- Prueba de usuario: login piloto OK, datos OK (mando 50%, formación OK);
  Laboratorio IA → `rate-limited` / `ai.proxy.enforcement` en `/training`
  (22:14 y 22:15 UTC). Causa 429 sin distinguir (falta body de `/api/ai`).

## Archivos que cambiaron (sin commit — pendiente)

- `docs/fase-5/cierre-fase-5.md`: estado → "cerrada con salvedad" + sección
  de cierre con salvedad.
- `docs/fase-6/plan-fase-6.md`: nuevo (backlog + contexto técnico).

## Próximo paso concreto (ítem 6.0)

Pedir al usuario el cuerpo de `POST /api/ai` (Status, `requestId`, `error`,
`source`, `provider`, `retry-after`, hora) + proveedor/modelo en Ajustes → IA.
Con eso diagnosticar 429 local vs cuota y aplicar fix en la misma sesión.
Si el usuario no lo tiene, re-ejecutar la prueba del Laboratorio con DevTools
abierto.

## Comandos útiles

- `supabase migration list --linked` (rápido, remoto).
- `python3 scripts/migration/test_platform_settings_etl.py` (6 tests, ~1 s).
- `npm run lint` con Node 20 (`/home/tata/.local/node-v20.20.2-linux-x64/bin`).
- Evitar `npm run quality` / `test:ci` completos en este entorno (timeouts);
  correr en máquina con más tiempo o por shards.
- `gh api repos/eltata71/arky-sup/actions/runs/<id>/jobs` para veredicto CI
  (repo real: `eltata71/arky-sup`, no `arkypro-1.0`).
