/**
 * Assisted capture — the vocabulary of "help me fill this in".
 *
 * Foundation layer: declarations only, no behaviour beyond two pure guards.
 * See `captureContracts.ts` for why the contract lives down here rather than
 * inside whichever module happened to need it first.
 */
export * from './captureContracts';
export * from './captureFields';
