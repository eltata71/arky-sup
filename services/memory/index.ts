/**
 * Structured agent-memory entries — the public surface of the `memory` module.
 *
 * See `services/chat/index.ts` for why this file exists.
 */
export * from './memoryEntries';
export * from './memoryScopes';
